class Block
    def __init__(self, previous_block_hash, merkle_root, transactions):
        self.previous_block_hash = previous_block_hash
        self.merkle_root = merkle_root
        self.time = self.time
        self.transactions = transactions 
